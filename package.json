{
  "name": "lowbandportfolio",
  "version": "1.0.0",
  "description": "My app",
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "21.x",
    "npm": "10.x"
  },
  "dependencies": {
  }
}
